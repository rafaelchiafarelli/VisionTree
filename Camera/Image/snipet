
import subprocess
from time import sleep
P = subprocess.Popen(['ping','127.0.0.1']) 
isDead = P.poll()
c = 0
while True:
    sleep(1)
    isDead = P.poll()
    print(isDead)
    c+=1
    if c > 10:
        P.kill()
        
